/**
 * PostCSS configuration file
 * This sets up Tailwind CSS as a PostCSS plugin.
 * @type {import('postcss-load-config').Config}
 */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
